var minhavar = 'minha variavel';

function minhaFunc(x, y){
    return x + y;
}

// ES6 ou ES2015
let num = 2;
const PI = 3.1415

var numeros = [1, 2 , 3];
numeros.map(valor => valor * 2 );

class Matematica{
    soma(x, y){
        return x+y;
    }
}
 let n1: string = '5';